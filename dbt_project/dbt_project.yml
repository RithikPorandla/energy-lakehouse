name: 'energy_lakehouse'
version: '1.0.0'
config-version: 2

profile: 'energy_lakehouse'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  energy_lakehouse:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: table
    marts:
      +schema: marts
      +materialized: table

seeds:
  energy_lakehouse:
    epa_frs_crosswalk:
      +schema: intermediate
      +column_types:
        CAMD_PLANT_ID: varchar
        FRS_ID: varchar
